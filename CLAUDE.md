Read @README.md to understand the project and setup.

When making changes to visualize.py (the map generation script), always verify that export.py still works afterward by running `python export.py --wards 1` and checking that the output screenshot looks correct.
